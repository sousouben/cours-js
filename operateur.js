//dans les opérateurs on a: + - * / **
let total = 0;
total = total + 1; // ou total ++ (incrémentation) ou +=
console.log(total); // 1

// tout le js est lu de droite à gauche donc toujours mettre les opérateurs pres  le = pour pouvoir incrémenter
